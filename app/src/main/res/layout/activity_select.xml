<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SelectActivity">

    <Button
        android:id="@+id/mapButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="44dp"
        android:text="현위치 지도 보기"
        app:layout_constraintBottom_toTopOf="@+id/currentLocationButton"
        app:layout_constraintEnd_toEndOf="@+id/anotherLocationButton"
        app:layout_constraintHorizontal_bias="0.471"
        app:layout_constraintStart_toStartOf="@+id/currentLocationButton" />

    <Button
        android:id="@+id/currentLocationButton"
        android:layout_width="130dp"
        android:layout_height="86dp"
        android:layout_marginStart="58dp"
        android:layout_marginTop="322dp"
        android:layout_marginEnd="47dp"
        android:layout_marginBottom="323dp"
        android:text="@string/select_current"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/anotherLocationButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/anotherLocationButton"
        android:layout_width="130dp"
        android:layout_height="86dp"
        android:layout_marginTop="322dp"
        android:layout_marginEnd="46dp"
        android:layout_marginBottom="323dp"
        android:text="@string/select_another"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>